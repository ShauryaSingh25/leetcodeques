class Solution {
public:
    bool isPalindrome(int x) {
        if(x<0){
            return false;

        }
        long r=0;
        int temp=x;
        while(temp!=0){
            r=10*r+temp%10;
            temp=temp/10;

        
        }

        return {r==x};
    }
};
